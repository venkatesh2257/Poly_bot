/** Full banner logo in `public/polypredator-logo.png` (served at site root). */
export const BRAND_LOGO_SRC = "/polypredator-logo.png";

/** Source file: `src/assets/site-background.png` (imported in `App.tsx` so Vite emits a working URL). */
export const BRAND_BG_SRC = "/site-background.png";
