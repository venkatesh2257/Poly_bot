# Optional smoke-test logs (no strategy change): SMOKE_TEST_LOGS=1
#
# Live anchor (python -m hf_anchor_bot): DRY_RUN=false requires a live executor.
# HF_ANCHOR_LIVE_EXECUTOR_AVAILABLE=true wires LiveOrderExecutor (replace with real CLOB in execution.py).
# DRY_RUN=false
